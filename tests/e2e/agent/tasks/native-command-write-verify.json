{
  "id": "native-command-write-verify",
  "title": "Native runtime verifies shell command workspace writes",
  "description": "Runs the native runtime against a scripted provider that writes a project file through run_command, then proves the host treats the command as a write side effect and runs active verification.",
  "capabilities": [
    "native_runtime_execution",
    "command_side_effect_verification",
    "active_verification_gate"
  ],
  "timeoutMs": 120000,
  "workspace": {
    "files": [
      {
        "path": "package.json",
        "content": "{\n  \"name\": \"funplay-agent-e2e-command-write-verify\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"scripts\": {\n    \"test\": \"node acceptance/command-write-verify.test.mjs\"\n  }\n}\n"
      },
      {
        "path": "acceptance/command-write-verify.test.mjs",
        "content": "import assert from 'node:assert/strict';\nimport { readFile } from 'node:fs/promises';\n\nconst output = JSON.parse(await readFile(new URL('../src/agent-output.json', import.meta.url), 'utf8'));\nassert.equal(output.ready, true);\nassert.equal(output.marker, 'FUNPLAY_NATIVE_COMMAND_OK');\nconsole.log('native-command-write-verify acceptance passed');\n"
      }
    ]
  },
  "agentCommand": {
    "command": "cd \"$FUNPLAY_REPO_ROOT\" && node --experimental-strip-types --import ./tests/register-ts-loader.mjs ./scripts/native-runtime-e2e.mjs command-write-verify \"$FUNPLAY_WORKSPACE_ROOT\"",
    "cwd": ".",
    "timeoutMs": 120000
  },
  "acceptance": [
    {
      "id": "acceptance-npm-test",
      "kind": "test",
      "command": "npm test",
      "required": true,
      "timeoutMs": 30000
    }
  ]
}
